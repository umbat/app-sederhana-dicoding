<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".DetailActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageView
                android:id="@+id/iv_details"
                android:layout_width="200dp"
                android:layout_height="312.5dp"
                android:layout_marginStart="16dp"
                android:layout_marginTop="16dp"
                android:layout_marginBottom="8dp"
                tools:srcCompat="@tools:sample/avatars" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tv_full_name_title"
                    style="@style/ShortDetailsTitle"
                    android:text="Full Name"/>

                <TextView
                    android:id="@+id/tv_full_name"
                    style="@style/ShortDetails"
                    tools:text="@string/players_full_name"/>

                <TextView
                    android:id="@+id/tv_place_of_birth_title"
                    style="@style/ShortDetailsTitle"
                    android:text="Place of Birth"/>

                <TextView
                    android:id="@+id/tv_place_of_birth"
                    style="@style/ShortDetails"
                    tools:text="@string/place_of_birth"/>

                <TextView
                    android:id="@+id/tv_time_of_birth_title"
                    style="@style/ShortDetailsTitle"
                    android:text="Time of Birth"/>

                <TextView
                    android:id="@+id/tv_time_of_birth"
                    style="@style/ShortDetails"
                    tools:text="@string/time_of_birth"/>

                <TextView
                    android:id="@+id/tv_position_title"
                    style="@style/ShortDetailsTitle"
                    android:text="Position"/>

                <TextView
                    android:id="@+id/tv_position"
                    style="@style/ShortDetails"
                    tools:text="@string/position"/>

            </LinearLayout>
        </LinearLayout>

        <TextView
            android:id="@+id/tv_details"
            style="@style/LongDetails"
            tools:text="@string/detail"/>

        <Button
            android:id="@+id/btn_link"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="24dp"
            android:layout_marginEnd="24dp"
            android:layout_marginBottom="24dp"
            android:text="Read More"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

    </LinearLayout>

</ScrollView>